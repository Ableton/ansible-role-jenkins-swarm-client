---
- name: Ensure Jenkins Swarm Client service is started
  become: true
  service:
    name: jenkins-swarm-client
    state: started
  # This is required or else molecule's idempotency test will fail. See:
  # https://github.com/ansible-community/molecule/issues/2765
  changed_when: false
